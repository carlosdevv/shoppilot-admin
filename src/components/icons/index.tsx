import { Check, ChevronsUpDown, Loader, LucideIcon, PlusCircle, Store, Trash } from "lucide-react";

export type IconType = LucideIcon;

export const Icons = {
  loading: Loader,
  store: Store,
  chevronsArrow: ChevronsUpDown,
  check: Check,
  addCircle: PlusCircle,
  trash: Trash,
};
